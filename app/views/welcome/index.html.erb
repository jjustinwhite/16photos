<body class="welcome">
	<div class="home intro-message">
	<span class="siteBackground"></span>
		<% if logged_in? %>
			<%= render 'signedin' %>		
		<% else %>
			<%= render 'signedout' %>
		<% end %>	
	</div>
</body>


