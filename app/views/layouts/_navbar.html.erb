<nav class="navbar navbar-default navbar-static-top">
  <div class="container">

    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "16photos", posts_path, :class => "navbar-brand" %>
    </div>

    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">     
        <% if logged_in? %>       
            <li class="<%= 'active' if current_page?(posts_path) %>"><%= link_to " Home", posts_path, :class => "fa fa-home" %></li>
            <li class="<%= 'active' if current_page?(new_post_path) %>"><%= link_to " New Photo", new_post_path, :class => "fa fa-camera-retro" %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%=  gravatar_for current_user, size: 20 %> <%= current_user.username %> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li class="<%= 'active' if current_page?(current_user) %>"><%= link_to " My Profile", current_user, :class => "fa fa-user" %></li>
                <li class="<%= 'active' if current_page?(edit_user_path(current_user)) %>"><%= link_to " Settings", edit_user_path(current_user), :class => "fa fa-gear" %></li>

                <li class="divider"></li>

                <% if current_user.admin? %>
                  <li class="<%= 'active' if current_page?(users_path) %>"><%= link_to " Users", users_path, :class => "fa fa-users" %></li>

                  <li class="divider"></li>
                <% end %>

                <li>
                  <%= link_to " Log out", logout_path, method: "delete", :class => "fa fa-sign-out" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to " Sign up", signup_path, :class => "fa fa-arrow-circle-o-up" %></li>
            <li><%= link_to " Log In", login_path, :class => "fa fa-sign-in" %></li>
          <% end %>
      </ul>
    </div><!--/.nav-collapse -->

  </div>
</nav>



